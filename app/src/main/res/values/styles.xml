<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <declare-styleable name="ProcessButton">
        <attr name="pb_textProgress" format="string" />
        <attr name="pb_textComplete" format="string" />
        <attr name="pb_textError" format="string" />
        <attr name="pb_colorProgress" format="color" />
        <attr name="pb_colorComplete" format="color" />
        <attr name="pb_colorError" format="color" />
    </declare-styleable>
    <declare-styleable name="FlatButton">
        <attr name="pb_colorPressed" format="color" />
        <attr name="pb_colorNormal" format="color" />
        <attr name="pb_cornerRadius" format="dimension" />
    </declare-styleable>

    <style name="rb_checked_style">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@drawable/txt_rb_black_gray_selector</item>
        <item name="android:button">@null</item>
        <item name="android:drawableTop">@dimen/dimen_5dp</item>
        <item name="android:textSize">@dimen/txtsize_26sp</item>
        <item name="android:paddingTop">@dimen/dimen_5dp</item>
    </style>

    <style name="tv_normal_style">
        <item name="android:textColor">@color/black_2c</item>
        <item name="android:textSize">@dimen/txtsize_30sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">end</item>
    </style>
    <style name="DialogStyleBottom" parent="android:Theme.Dialog">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

</resources>